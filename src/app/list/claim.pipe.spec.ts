/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ClaimPipe } from './claim.pipe';

describe('Pipe: Claim', () => {
  it('create an instance', () => {
    let pipe = new ClaimPipe();
    expect(pipe).toBeTruthy();
  });
});
